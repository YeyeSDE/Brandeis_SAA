<div class="main-content">
    <article>
      <p class="title">Current Available Jobs</p>
      <p class="introduction"> We are currently seeking a highly motivated individual to join our team as an employee! </p>
      <p class="introduction"> The ideal candidate should possess excellent communication and organizational skills, be able to work effectively in a team environment, and have a strong work ethic.</p>
      <table class="table">
        <thead>
          <tr>
            <th>Job Title</th>
            <th>Company</th>
            <th>Location</th>
            <th>Operations</th>
          </tr>
        </thead>
        <tbody>
          <% jobs.forEach(job => { %>
          <tr>
            <td><a href="<%= `/jobs/${job._id}`%>"><%= job.title %></a></td>
            <td><%= job.company %></td>
            <td><%= job.location %></td>

            <td>
              <a class="buttons-1" href="<%=`/jobs/${job._id}/edit`%>"> Edit </a>
              &nbsp;
              <a class="buttons-1" href="<%= `jobs/${job._id}/delete?_method=DELETE` %>"
                onclick="return confirm('Confirm to delete this record?')">Delete</a>
            </td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      <br>
      <a href="jobs/new" class="main-button">Post a New Job</a>
    </article>
</div>


